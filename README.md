# FolderVault
An incremental folder-backup app for Android.

## Local setup

After cloning the repo, add the Firebase configuration file before building:

```
app/google-services.json
```

This file is excluded from version control (`.gitignore`) because it contains project-specific Firebase keys.
Obtain it from the Firebase console under **Project settings → Your apps → google-services.json**.
